# BallAndBeam
A PID controller for a premade ball and beam platform. The PID controller was implemented using Arduino framework. The input data was the measured distance from one end of the beam to the ball using an IR sensor.
